const c=document.getElementById('particles'),ctx=c.getContext('2d');function rs(){c.width=innerWidth;c.height=innerHeight}rs();addEventListener('resize',rs);let m={x:-999,y:-999};addEventListener('mousemove',e=>m={x:e.clientX,y:e.clientY});let pts=[...Array(90)].map(()=>({x:Math.random()*c.width,y:Math.random()*c.height,vx:(Math.random()-.5)*.25,vy:(Math.random()-.5)*.25}));function d(){ctx.clearRect(0,0,c.width,c.height);for(let i=0;i<pts.length;i++){let a=pts[i];a.x+=a.vx;a.y+=a.vy;if(a.x<0||a.x>c.width)a.vx*=-1;if(a.y<0||a.y>c.height)a.vy*=-1;ctx.fillStyle='#a7d8ff';ctx.beginPath();ctx.arc(a.x,a.y,2,0,7);ctx.fill();for(let j=i+1;j<pts.length;j++){let b=pts[j],dx=a.x-b.x,dy=a.y-b.y,dist=Math.hypot(dx,dy);if(dist<110){ctx.strokeStyle='rgba(125,211,252,'+(1-dist/110)*0.15+')';ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke();}}}requestAnimationFrame(d)}d();